/**
 * @param {number[]} piles
 * @return {boolean}
 */

const stoneGame = (piles) => {
  return true;
  // let alice = 0;
  // let bob = 0;

  // let turn = 0;
  // while (piles.length) {
  //   let points;
  //   if (piles[0] > piles[piles.length - 1]) {
  //     points += piles.shift();
  //   } else {
  //     points += piles.pop();
  //   }

  //   if (turn % 2 == 0) {
  //     alice += points;
  //   } else {
  //     bob += points;
  //   }

  //   turn++;
  // }

  // return alice > bob;
};
